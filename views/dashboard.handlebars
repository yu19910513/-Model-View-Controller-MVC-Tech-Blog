<div>

<h2 class="title">Dashboard: Your Posts</h2><hr>
{{#if posts.length}}
    <div>
    {{#each posts as |post|}}
        {{> post-info post}}
    {{/each}}
    </div>
{{else}}
<h3>You do not have any Posts yet.</h3>

{{/if}}
